module.exports = {
    CityRespository : require('./city_respository')
}

// This File is just to export all the repositories in one go 
// if there were multiple repositroy we can export all of them at once 